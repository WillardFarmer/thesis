\ProvidesPackage{preamble}
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage[backend=biber,style=ieee,citestyle=ieee,dashed=false]{biblatex}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{wasysym}
\usepackage{bm}
\usepackage{graphicx}	    % Use for figures
%\usepackage{subfig}	    % Use for Sub Figures
\graphicspath{{./Figures/}}
\usepackage{array}
\usepackage{caption}
\usepackage{subcaption} %subcaption cannot be used in tandem with subfig

\usepackage{booktabs}
\usepackage{tabularx}
\usepackage[table,xcdraw]{xcolor}
\usepackage{multirow}
\usepackage{blindtext}
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    filecolor=gray,      
    urlcolor=black,
    linkbordercolor=gray,
    pdftitle={Willard_Farmer_202307_MASc_Thesis},
    citecolor=black,
    pdfpagemode=FullScreen,
    }

\urlstyle{same}

\addto\extrasenglish{%
  \renewcommand{\subsectionautorefname}{Section}%
}


\usepackage{comment}
\usepackage{textcomp}
\usepackage{gensymb}
\usepackage{csquotes}
\pdfminorversion=7 % this must be before the next two
\usepackage[T2A]{fontenc} % this is to make sure the pdf is ctrl-fable
\usepackage[acronym,nopostdot,nonumberlist,nogroupskip]{glossaries}

\usepackage{makecell}
\usepackage{threeparttable}
\usepackage[titletoc]{appendix}
%\usepackage{url}
%\usepackage{siunitx}
\usepackage{xparse}	    % For the notation-shortcuts 
\usepackage{algorithm}	    % For algorithm pseudo-code
\usepackage{algpseudocode}  % For algorithm pseudo-code
\usepackage{varwidth}       % Center align enumerate

% test tikz 3d globe package
%\usepackage{tikz-3dplot}

% Code
\usepackage{listings}


%% *** Acronyms page (gls) setup. ***
\setacronymstyle{long-short}
\setglossarystyle{alttree} % Lines up the definitions
\glssetwidest{LONGLONG} % Sets the spacing between the acronym and definition
% \makeglossaries
\usepackage{makecell}
\usepackage{threeparttable}
\usepackage[titletoc]{appendix}
%\usepackage{url}
%\usepackage{siunitx}
\usepackage{xparse}	    % For the notation-shortcuts 
\usepackage{algorithm}	    % For algorithm pseudo-code
\usepackage{algpseudocode}  % For algorithm pseudo-code
\usepackage{varwidth}       % Center align enumerate
\usepackage{soul}	    % Highlight
\usepackage{outlines}       % Items/SubItems in bulletpoint list
\usepackage{arydshln}       % Dashed Matrix line
\usepackage{commath}        % Norm Symbol


% test tikz 3d globe package
%\usepackage{tikz-3dplot}

% Code
\usepackage{minted}
% \setsansfont{Calibri}
% \setmonofont{Consolas}



%% *** Acronyms page (gls) setup. ***
\setacronymstyle{long-short}
\setglossarystyle{alttree} % Lines up the definitions
\glssetwidest{LONGLONG} % Sets the spacing between the acronym and definition
% \makeglossaries
